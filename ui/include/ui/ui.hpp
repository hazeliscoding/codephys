#pragma once

// Phase 0 stub for the UI module (DESIGN §7.3). Wired but minimal: the real immediate-mode
// widgets land in Phase 1. ui depends downward only on render.

namespace ui {

// Placeholder public symbol; replaced by the immediate-mode UI facade in Phase 1.
const char* module_name();

}  // namespace ui

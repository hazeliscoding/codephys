// doctest entry point. Defining this in its own TU keeps the (large) implementation out
// of the per-test files and lets them compile fast.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
